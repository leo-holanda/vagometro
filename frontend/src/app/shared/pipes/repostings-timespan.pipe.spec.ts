import { RepostingsTimespanPipe } from './repostings-timespan.pipe';

describe('RepostingsTimespanPipe', () => {
  it('create an instance', () => {
    const pipe = new RepostingsTimespanPipe();
    expect(pipe).toBeTruthy();
  });
});
