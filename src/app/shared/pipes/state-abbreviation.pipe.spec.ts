import { StateAbbreviationPipe } from './state-abbreviation.pipe';

describe('StateAbbreviationPipe', () => {
  it('create an instance', () => {
    const pipe = new StateAbbreviationPipe();
    expect(pipe).toBeTruthy();
  });
});
